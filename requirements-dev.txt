# ─────────────────────────────────────────────────────────────────────────
# Duck Hunt — залежності для розробки та CI/CD
# ─────────────────────────────────────────────────────────────────────────
#
# Включає всі runtime-залежності + інструменти тестування та лінтингу.
#
# Встановлення:
#   pip install -r requirements-dev.txt
# ─────────────────────────────────────────────────────────────────────────

# ── Runtime (включаємо основний файл) ────────────────────────────────────
-r requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.3.5              # фреймворк для тестів
pytest-html==4.1.1         # HTML-звіт: pytest --html=reports/test-report.html

# ── Linting ───────────────────────────────────────────────────────────────
flake8==7.1.1              # перевірка стилю PEP 8
flake8-html==0.4.3         # HTML-звіт для flake8

# ── Build (опційно, для етапу Build у CI/CD) ─────────────────────────────
pyinstaller==6.11.1        # пакування у standalone-бінарник
